<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.hstc.recommend.dao.MenuDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="cn.hstc.recommend.entity.MenuEntity" id="menuMap">
        <result property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="url" column="url"/>
        <result property="icon" column="icon"/>
        <result property="permission" column="permission"/>
        <result property="type" column="type"/>
    </resultMap>

    <select id="queryMenuNames" resultType="String">
        select menu.name from menu
        <where>
                ${ew.sqlSegment}
        </where>
    </select>

    <select id="queryMenuIds" resultType="Integer">
        select menu.id from menu
        <where>
                ${ew.sqlSegment}
        </where>
    </select>

    <!-- 查询用户的所有权限 -->
    <select id="queryAllMenuByUser" resultMap="menuMap" parameterType="int">
        SELECT m.id,m.parent_id,m.name,m.url,m.icon FROM user_role ur
        LEFT JOIN role_menu rm ON ur.role_id = rm.role_id
        LEFT JOIN menu m ON m.id = rm.menu_id
        <where>
            ur.user_id = #{userId}
        </where>
    </select>

    <select id="queryMenuIdsByRoleId" resultMap="menuMap" parameterType="int">
        select menu.id,menu.name from menu
        left join role_menu rm on menu.id = rm.menu_id
            <where>
                rm.role_id = #{roleId}
            </where>
    </select>
</mapper>